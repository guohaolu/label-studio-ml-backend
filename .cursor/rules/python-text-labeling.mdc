---
description: Python 新手友好规范——类型标注、注释；群聊 NER；业务代码在 my_ml_backend
globs: "**/*.py"
alwaysApply: true
---

# 项目聚焦（不要偏题）

- 本仓库用于 **Label Studio ML Backend**：把模型封装成可被 Label Studio 调用的服务。本项目的核心任务是 **命名实体识别（NER）**：从文本中标出实体跨度与类型，并支持 **预标注 / 交互标注 / 训练** 等与 NER 相关的流程。
- **数据来源与场景**：输入主要是 **业务聊天群里的对话消息**（例如催单、预约、售后沟通等多轮口语化内容），不是通用新闻或维基式长文。设计分词、截断、提示词或后处理时，应贴合 **短句、碎片、多人插话** 等特点。
- **文本形态**：消息里常夹杂大量 **编码类、标识类** 片段，例如 **SKU**、**匿名化后的用户昵称或 ID**、**快递面单号 / 运单号**、订单号、门店编码等。注释与类型设计时应体现「实体边界可能紧贴标点与数字字母混排」「同一类编码在不同消息中格式可能不统一」等现实约束；不要随意「清洗掉」可能对 NER 有区分度的字符，除非业务明确要求。
- 除非用户明确要求，不要主动把方案引向与本场景无关的主线（例如纯图像检测、视频分割、与本群聊 NER 无关的通用分类大赛流程）。若涉及 OCR/ASR，仅作为 **群聊文本的获取方式**，最终标注对象仍是 **文本上的 NER**。
- **当前业务代码位置**：本项目的 ML Backend 实现在 **`my_ml_backend/`**（如 `model.py`、`_wsgi.py`、测试等）。新增或修改 NER 相关逻辑时，**默认只改 `my_ml_backend`**，并与其中已有用法保持一致。
- **`label_studio_ml`** 视为上游 SDK / 依赖包：除非你要给整个仓库提通用改动，否则 **不要** 默认去改 `label_studio_ml/examples/` 下各类示例；需要参考实现时，可阅读示例再把思路落到 `my_ml_backend`。
- 修改代码时优先对齐 **Label Studio 的数据结构**（如 `tasks`、`predictions`、`completion`、标注 JSON），并遵循 `label_studio_ml` 对 ML Backend 的接口约定。

---

# 类型标注（必须）

**原因**：Python 运行时不会强制类型，IDE 和阅读者都依赖注解来理解「变量里是什么」。

- 为 **函数参数与返回值** 写全类型；模块级 **复杂变量** 也建议注解。
- 容器写明元素类型，例如 `list[str]`、`dict[str, Any]`；可选值用 `str | None` 或 `Optional[str]`（与项目现有风格一致即可）。
- 在 Python 3.9+ 可用内置小写泛型（`list[str]`）；若需兼容更旧版本，与仓库现有代码保持一致。
- 难以精确时，用 `typing` / `collections.abc` 中合适的抽象（如 `Mapping[str, Any]`）并在注释里说明实际形状。

**示例（好）**

```python
def split_sentences(text: str, max_len: int = 512) -> list[str]:
    ...
```

**示例（差）**

```python
def split_sentences(text, max_len=512):
    ...
```

---

# 注释与文档语言（必须）

- **代码注释必须使用简体中文**（`# …`、`"""…"""` 中面向阅读者的说明；已有英文历史遗留可逐步改为中文，**新增与修改的注释不得再写英文说明**）。原因：团队阅读与评审以中文为主，避免中英混用导致理解成本上升。
- **面向团队的说明文档**（尤其是 `my_ml_backend/README.md`）**必须使用简体中文**，并与实现保持同步。
- **原因（注释要写什么）**：代码只写「怎么做」；注释补 **业务含义、数据格式、设计取舍**；若与 **群聊消息或 SKU/单号类实体** 相关，注明业务含义与边界情况。
- **非显而易见**的逻辑必须注释：输入输出在 Label Studio 里长什么样、为什么要这样解析或拼接。
- 避免复述代码字面意思（例如不要只写「循环列表」）；对易混淆的字段，用一行 **mini 示例**（JSON 片段或伪数据）说明即可。
- **标签与匹配策略**：每增加或改动一个 Label Studio 实体标签（`labels` 中的取值），必须在 **`my_ml_backend/README.md`** 的「每个标签的匹配策略」中 **逐标签写清**：匹配手段（正则 / 字典 / 数据库校验 / 模型等）、关键模式或数据来源、与 Doris 等外部系统的契约、已知误报/漏报与可选环境变量。**禁止只改代码不更新 README。**

**示例（好）**

```python
# 原因：Label Studio 的 task 里文本可能在 "data.text" 或 "data" 的其它键中；
# 这里统一取出 str，避免下游模型收到非字符串类型。
raw = task.get("data") or {}
text = raw.get("text") or raw.get("body", "")
```

---

# 协作方式（给 AI 的约束）

- 新增或修改 Python 时：**先补类型与关键注释，再写实现**；不要留「无类型、无说明」的大段逻辑。默认改动路径为 **`my_ml_backend/`**。
- 解释设计时，可简短说明 **与群聊 NER 标注流程** 的关系（预标、交互、训练回调、实体标签体系与编码类字段等）。
- 不要为「显而易见的一行」写冗长 docstring；把笔墨放在 **边界情况、数据格式、与 Label Studio 的契约** 上。
- 日志文案若项目里已统一为英文，可继续与现有风格一致；**与「注释」区分开**，注释仍以中文为准。
